spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taphoadb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      # auto update db
      ddl-auto: update